# SelfDiary — Infrastructure

This directory contains production infrastructure configuration files.

## Contents

| Directory | Purpose |
|-----------|---------|
| `docker/` | Additional Dockerfiles or build configs (if not colocated) |
| `nginx/`  | Nginx reverse proxy configuration for production |

These files are used during Phase 11 (Deployment).
