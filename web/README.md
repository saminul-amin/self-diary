# SelfDiary — Web Client

Next.js application with TypeScript, Tailwind CSS, TanStack Query, and Axios.

## Setup

```bash
npm install
npm run dev
```

## Structure

See top-level README.md for the full directory layout.
This directory will be scaffolded with `create-next-app` during Phase 7 (Web Client).
